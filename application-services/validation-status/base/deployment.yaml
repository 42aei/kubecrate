apiVersion: apps/v1
kind: Deployment
metadata:
  name: validation-status
  labels:
    app.kubernetes.io/name: validation-status
    app.kubernetes.io/part-of: kubecrate
    kubecrate.io/workload-category: application-services
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: validation-status
  template:
    metadata:
      annotations:
        validation-status.kubecrate.io/config-revision: "overview-v1"
      labels:
        app.kubernetes.io/name: validation-status
        app.kubernetes.io/part-of: kubecrate
        kubecrate.io/workload-category: application-services
    spec:
      serviceAccountName: validation-status
      containers:
        - name: app
          image: python:3.13-alpine
          imagePullPolicy: IfNotPresent
          command: ["python", "/app/app.py"]
          env:
            - name: VALIDATION_STATUS_CONFIG
              value: /config/config.json
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /status.json
              port: http
            initialDelaySeconds: 2
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /status.json
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              cpu: 100m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
          volumeMounts:
            - name: app
              mountPath: /app
              readOnly: true
            - name: config
              mountPath: /config
              readOnly: true
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        seccompProfile:
          type: RuntimeDefault
      volumes:
        - name: app
          configMap:
            name: validation-status-app
        - name: config
          configMap:
            name: validation-status-config
